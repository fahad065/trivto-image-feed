# Unsplash API access key (server-side only — never exposed to the client).
# Get one free at https://unsplash.com/developers -> "New Application".
UNSPLASH_ACCESS_KEY=

# MongoDB connection string (e.g. from a free MongoDB Atlas cluster).
# Used to persist Like state so it survives a page refresh.
MONGODB_URI=
